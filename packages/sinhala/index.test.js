// Basic smoke test - can be expanded with a test runner
const f = require('./index');
console.assert(typeof f.format === 'function', 'format should be a function');
console.log('sinhala tests: OK');
