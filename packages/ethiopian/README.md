# Ethiopian Formatter

Simple Ethiopian (Amharic) date formatter.

Note: A true Ethiopian calendar implementation is more complex; this is a locale-based starter.

Usage:

```js
const { format } = require('./index');
console.log(format(new Date()));
```
