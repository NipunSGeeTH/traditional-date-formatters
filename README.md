# traditional-date-formatters

Collection of small, locale-aware date formatter packages for different language communities.

Quickstart:

```bash
# Run the example that prints each formatter's output
npm run examples
```

Contributing: see `docs/CONTRIBUTING.md` and `docs/adding-formatter.md`.
